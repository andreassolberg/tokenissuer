{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "dataporten-tokenissuer"
    },
    "spec": {
        "selector": {
            "app": "dataporten-tokenissuer"
        },
        "ports": [{
            "protocol": "TCP",
            "port": 3000,
            "targetPort": 3000
        }],
        "type": "NodePort"
    }
}